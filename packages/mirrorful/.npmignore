# If a project has both an .npmignore and .gitignore file, npm will only use the .npmignore file.
# https://docs.npmjs.com/cli/v9/using-npm/developers#keeping-files-out-of-your-package

INTERNAL_README.md

# NOTE: we purposely do not list .next here, because we want to publish it to npm

# No need for this, as it's already built in the .next package
editor/src

# No need for this, as it's all in dist
*.ts