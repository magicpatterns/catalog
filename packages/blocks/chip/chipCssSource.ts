export const labelChipConstantCssTemplate = `.{{ORG}}-chip {
    padding: 4px 6px;
    display: flex;
    align-items: center;
    font-weight: 600;
    border-radius: 4px;
  
    .{{ORG}}-chip-right-icon {
      margin-right: 8px;
    }
  
    .{{ORG}}-chip-left-icon {
      margin-left: 8px;
    }
  }
`

export const labelChipVariantCssTemplate = `.{{VARIANT_CLASS_NAME}} {
    color: {{textColor}};
    background-color: {{backgroundColor}};
}
`
