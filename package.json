{
  "private": "true",
  "workspaces": [
    "packages/*",
    "apps/*",
    "packages/mirrorful/editor/"
  ],
  "scripts": {
    "postinstall": "yarn --cwd server --production=false; yarn --cwd server build"
  }
}
