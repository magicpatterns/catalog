---
title: '💿 Export Formats'
description: 'After configuring your theme in the editor, you can export it to be used by your app.'
---

We currently export to the following file types: `.js`, `.ts`, `.css`, `.scss`, `.json`. If you're using tailwind, we also export to common js to work nicely with your tailwind config.

## From Web

If you are using Mirrorful's web editor, you will need to create the imported files yourself. You can do this by simply creating a file of your desired format (i.e. `.js`, `.css`, etc) and then copying
the generated code into the file.

## Colors

### Using CSS

We export to CSS as CSS variables. CSS variables are the easiest way to create and set variables across CSS. They are supported in all modern browsers, and can be used in any CSS file.

To get your tokens, simply import Mirrorful's `.css` file into your project. _If you are using Mirrorful Web, you will need to create this file yourself._

For example, in your main app:

```javascript
/* Exact path will depend on your file structure */
import './.mirrorful/theme.css'

export default function Root() {
  return <App />
}
```

Then in any CSS file (or inline Javascript), you can reference CSS variables like so:

```css
.primary-button {
  background-color: var(--color-primary);
}
```

```javascript
<button style={{ backgroundColor: 'var(--color-primary)'}}>
```

### Using SCSS

In addition to CSS variables, we also export to SCSS variables.

In order to access SCSS variables, you will need to import Mirrorful's `.scss` file into **each** stylesheet you want to use the variables in.

For example,

```scss
/* Exact path will depend on your file structure */
import './.mirrorful/theme.scss' .primary-button {
  background-color: $color-primary;
}
```

### Using Javascript / Typescript

If your project prefers inline styles, or want to use Mirrorful's tokens in Javascript, we also export to Javascript and Typescript.

```javascript
/* Exact path will depend on your file structure */
import { Tokens } from './mirrorful.js'

<button backgroundColor={{ Tokens.primary.base }}>Click here</button>
```

### Tailwind

If you projects uses Tailwind you can extend the color theme using the Mirrorful generated tokens.
_Exact path will depend on your file structure_

```javascript
const mirrorful = require('./.mirrorful/theme_cjs.js')

theme: {
  extend: {
    colors: Tokens.colors
  }
}
```

## Font Sizes

### Using CSS

```css
.headings {
  font-size: var(--font-size-lg);
}
```

```javascript
<button style={{ fontSize: 'var(--font-size-sm)'}}>
```

### Using SCSS

```scss
/* Exact path will depend on your file structure */
import './.mirrorful/theme.scss';

.primary-button {
  font-size: $font-size-sm;
}
```

### Using Javascript / Typescript

```javascript
/* Exact path will depend on your file structure */
import { Tokens } from './mirrorful.js'

<button fontSize={{ Tokens.fontSizes.sm }}>Click here</button>
```

### Tailwind

## Shadows

### Using CSS

```css
.headings {
  box-shadow: var(--box-shadows-sm);
}
```

```javascript
<button style={{ boxShadow: 'var(--box-shadows-sm)'}}>
```

### Using SCSS

### Using Javascript / Typescript

### Tailwind

## Examples

We have example projects for reference.

- [With Create React App](https://github.com/Mirrorful/mirrorful/tree/main/examples/create-react-app)
- [With Next.js](https://github.com/Mirrorful/mirrorful/tree/main/examples/with-chakra-ui)
- [With Tailwind](https://github.com/Mirrorful/mirrorful/tree/main/examples/tailwind-next)
- [With Chakra UI](https://github.com/Mirrorful/mirrorful/tree/main/examples/with-chakra-ui)
- [With Nuxt 3](https://github.com/Mirrorful/mirrorful/tree/main/examples/nuxt-3)
